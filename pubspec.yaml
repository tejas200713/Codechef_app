name: project
description: "A new Flutter project."


publish_to: 'none' # Remove this line if you intend to publish the package.

version: 1.0.0+1

environment:
  sdk: ">=2.12.0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter
  provider: ^6.0.0
  http: ^0.13.4
  flutter_map: ^0.14.0
  latlong2: ^0.8.1
  geolocator: ^8.2.0
  geocoding: ^2.0.0          # Add this line for geocoding
  flutter_native_splash: ^2.0.3
dev_dependencies:
  flutter_test:
    sdk: flutter

# The following section is optional and allows you to specify
# the Flutter SDK version you want to use.
flutter:
  uses-material-design: true
